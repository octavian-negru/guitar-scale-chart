[project]
name = "guitar-scale-chart"
version = "0.1.0"
description = "Generate terminal and PDF guitar fretboard charts"
requires-python = ">=3.10"
dependencies = [
    "reportlab>=4.0,<5.0",
]

[tool.uv]
package = false